{
  "name": "kind-ledger",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "seed": "pnpm -C apps/api seed",
    "migrate": "pnpm -C apps/api prisma:migrate",
    "generate": "pnpm -C apps/api prisma:gen"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}